parameters:
    git_dir: .
    bin_dir: vendor/bin
    ascii: ~
    tasks:
        composer: ~
        git_blacklist:
            keywords:
                - "die("
                - "var_dump("
                - "exit;"
        phpcpd:
            exclude: ['tests' ,'vendor']
        phpcs: ~
        phpmd:
            whitelist_patterns:
                - /^src\/(.*)/
            ruleset:
                - phpmd.xml
        phpunit: ~
        phpversion:
            project: '7.2'
        securitychecker: ~
        xmllint: ~
        yamllint: ~
        git_commit_message:
            allow_empty_message: false
            max_subject_width: 72
        git_branch_name:
            whitelist:
                Git-flow: /(master|develop|feature\/(\w+-)?\d+|(release|hotfix)\/(\d+\.\d+\.\d+))/
